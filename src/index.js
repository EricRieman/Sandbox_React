// Creating a ToDo app by making an App coponent render 3 check boxes with 
// paragraphs next to them

import ReactDOM from "react-dom"
import App from "./App"

ReactDOM.render( <App />, document.getElementById("root") );